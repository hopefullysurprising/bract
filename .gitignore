target
.vscode
.claude
.DS_Store

# insta: pending (unreviewed) snapshots — accepted ones (*.snap) are committed
*.snap.new
*.pending-snap

tmp

# Local task roster (mustered into the supereverything map, never committed)
TASKS.org
